[build]
  base = "frontend/"
  publish = "build/"
  command = "CI=false npm run build"

[build.environment]
  NODE_VERSION = "16.13.0"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
